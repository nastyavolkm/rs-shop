import { ColorizeAmountDirective } from './colorize-amount.directive';

describe('ColorizeAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizeAmountDirective();
    expect(directive).toBeTruthy();
  });
});
